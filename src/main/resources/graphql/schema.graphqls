type Query {
    login(loginData: LoginData!): JwtToken
    register(registerData: RegisterData!) : JwtToken
    popularArticles: [BlogArticle]
}

type BlogArticle {
    title: String
    slug: String
    description: String
    content: String
    likes: Int,
    author: BlogUser
}

type BlogUser {
    username: String
}

input LoginData {
    nameOrEmail: String!
    password: String!
}

input RegisterData {
    username: String!
    email: String!
    password: String!
}

type JwtToken {
    token: String!
}